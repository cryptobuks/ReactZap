import React from 'react';
import { AppRegistry } from 'react-native';
import App from './src/App';

const ReactZap = () => (
  <App />
);

AppRegistry.registerComponent('ReactZap', () => (ReactZap));
